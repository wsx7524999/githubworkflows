# PKI directories containing private keys - never commit
root/
intermediate/
server/
client/

# Private keys - never commit
*.key

# Certificate signing requests and serial files
*.csr
*.srl

# Note: foundation-trust-bundle.pem contains only public certificates
# and is safe to commit if needed for distribution
